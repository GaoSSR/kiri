{
  "name": "@gaossr/kiri",
  "version": "0.1.0",
  "description": "Kiri CLI for inspecting local development ports; installs the ports command",
  "license": "MIT",
  "private": true,
  "bin": {
    "ports": "./bin/ports.js"
  },
  "files": [
    "bin/",
    "lib/",
    "vendor/",
    "README.md"
  ],
  "scripts": {
    "check": "node --check bin/ports.js && node --check lib/resolve-binary.js",
    "pack:dry-run": "npm pack --dry-run"
  },
  "engines": {
    "node": ">=18"
  }
}
